name: Trigger auto deployment for container-app-1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-1-AutoDeployTrigger-80a65a1e-0b60-4122-86bf-ab0a6415d764.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CONTAINERAPP1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP1_REGISTRY_PASSWORD }}
          containerAppName: container-app-1
          resourceGroup: resource-group-1
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


